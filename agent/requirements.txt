# Core LLM & Orchestration
rich>=13.0.0
pyyaml>=6.0.0
langchain>=1.0.0,<2
langchain-core>=1.0.0,<2
langchain-openai>=1.0.0,<2
langgraph>=1.0.10,<1.1
langgraph-checkpoint>=2.1.0,<5
python-dotenv>=1.0.0
httpx>=0.28.0
oauth-cli-kit>=0.1.3

# Data & Computation
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Optional Analytics Storage
# Install with `pip install ".[analytics]"` if DuckDB-backed workflows are added.

# Document Readers (doc_reader_tool)
openpyxl>=3.1.0
python-docx>=1.1.0
python-pptx>=0.6.23
pypdfium2>=4.0.0
Pillow>=10.0.0

# ML Strategy
scikit-learn>=1.3.0
joblib>=1.3.0

# Optional Signal Skills
# Install with `pip install ".[signal-skills]"` when running the SMC or
# pyharmonics-backed harmonic example engines.

# Data Providers
requests>=2.31.0

# Optional Data Providers
# Install with `pip install ".[market-data]"` to enable Tushare, yfinance,
# AKShare, and CCXT provider integrations.

# REST API & SSE
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.18
sse-starlette>=1.6.0

# MCP Server
fastmcp>=2.0.0

# Web Search
ddgs>=6.0.0

# Shadow Account HTML/PDF Report
jinja2>=3.1.0
matplotlib>=3.7.0
weasyprint>=60.0
